To compile this program(assuming visual studio Community 2017 is installed) double click on Portfolio.sln and compile the project titled: "Temporal AA". All other related projects should be unloaded by default.

This project also makes use of Cmake in the case that the tester has a different version of visual studio installed.

to run the executable itself, open the folder titled TAA Standalone and follow the README file in that folder